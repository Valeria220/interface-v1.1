import React from 'react';
// Импорт основного компонента интерфейса поиска магазинов
import { StoreSearchUI } from './components/StoreSearchUI';
// Импорт стилей приложения
import './App.css';

/**
 * Главный компонент приложения - корневой компонент React-приложения
 * Служит точкой входа для рендеринга всего пользовательского интерфейса
 * 
 * @component
 * @function
 * @returns {JSX.Element} Основную структуру приложения
 * 
 * @example
 * // Использование в index.js:
 * // ReactDOM.render(<App />, document.getElementById('root'));
 */
function App() {
  return (
    /**
     * Основной контейнер приложения
     * Класс 'App' используется для глобальных стилей и позиционирования
     */
    <div className="App">
      {/**
       * Рендеринг основного компонента поиска магазинов
       * StoreSearchUI содержит всю бизнес-логику и интерфейс поиска товаров
       * Это единственный компонент, так как приложение сфокусировано на одной функции
       */}
      <StoreSearchUI />
    </div>
  );
}

// Экспорт компонента для использования в других частях приложения
// Является корневым компонентом, который рендерится в DOM
export default App;